---
phase: 04-frontend-paineis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/lib/api.ts
  - frontend/components/MetricCard.tsx
  - frontend/components/StatusCard.tsx
  - frontend/components/Skeleton.tsx
  - frontend/components/Banner.tsx
  - frontend/components/EmptyState.tsx
  - frontend/app/globals.css
autonomous: true
requirements: [FE-05, FE-06]

must_haves:
  truths:
    - "lucide-react e recharts estao instalados e importaveis"
    - "Componentes compartilhados renderizam sem erro de TypeScript"
    - "api.ts expoe endpoints para queue, accounts, personas, playground e logs"
  artifacts:
    - path: "frontend/components/MetricCard.tsx"
      provides: "Card de metrica reutilizavel (M5 e M6)"
      exports: ["MetricCard"]
    - path: "frontend/components/StatusCard.tsx"
      provides: "Card de status com left-border colorida (M6)"
      exports: ["StatusCard"]
    - path: "frontend/components/Skeleton.tsx"
      provides: "Skeleton loading animado"
      exports: ["Skeleton"]
    - path: "frontend/components/Banner.tsx"
      provides: "Banner de sucesso/erro temporario"
      exports: ["Banner"]
    - path: "frontend/components/EmptyState.tsx"
      provides: "Estado vazio com icone e texto"
      exports: ["EmptyState"]
    - path: "frontend/lib/api.ts"
      provides: "Cliente HTTP completo com todos os endpoints"
      exports: ["queueApi", "accountsApi", "personasApi", "playgroundApi", "logsApi"]
  key_links:
    - from: "frontend/components/MetricCard.tsx"
      to: "frontend/app/globals.css"
      via: "CSS variables"
      pattern: "var\\(--color-"
    - from: "frontend/lib/api.ts"
      to: "backend API"
      via: "fetch HTTP"
      pattern: "request<"
---

<objective>
Instalar dependencias (lucide-react, recharts), criar componentes UI compartilhados e estender api.ts com todos os endpoints necessarios para M5 e M6.

Purpose: Fundacao que todos os outros planos consomem — sem isto, nenhuma tela funciona.
Output: Componentes reutilizaveis, api.ts completo, deps instaladas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-paineis/04-UI-SPEC.md
@frontend/app/globals.css
@frontend/lib/api.ts
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependencias e estender api.ts com endpoints faltantes</name>
  <files>frontend/package.json, frontend/lib/api.ts</files>
  <read_first>
    - frontend/package.json (deps atuais)
    - frontend/lib/api.ts (endpoints existentes — analyticsApi e healthApi)
  </read_first>
  <action>
1. No diretorio `frontend/`, rodar:
   ```
   npm install lucide-react recharts
   ```
   NAO instalar @types/recharts — recharts 2.x inclui tipos.

2. Estender `frontend/lib/api.ts` adicionando os seguintes blocos APOS o bloco `healthApi`, mantendo TUDO que ja existe intacto:

**Interfaces novas (adicionar antes dos novos api objects):**

```typescript
// ── Queue ────────────────────────────────────────────────────

export interface QueueStats {
  waiting: number;
  active: number;
  failed: number;
  completed: number;
}

export const queueApi = {
  getStats: (token: string) =>
    request<{ data: QueueStats }>('/queue/stats', { token }),

  getJobs: (token: string, params?: { status?: string; page?: number; pageSize?: number }) => {
    const qs = new URLSearchParams();
    if (params?.status) qs.set('status', params.status);
    if (params?.page !== undefined) qs.set('page', String(params.page));
    if (params?.pageSize !== undefined) qs.set('pageSize', String(params.pageSize));
    const query = qs.toString() ? `?${qs.toString()}` : '';
    return request<{ data: QueueJob[]; total: number }>(`/queue/jobs${query}`, { token });
  },
};

// ── Accounts ─────────────────────────────────────────────────

export interface InstagramAccount {
  id: string;
  igUserId: string;
  username: string;
  tokenStatus: 'active' | 'expiring' | 'expired';
  tokenExpiresAt: string;
  createdAt: string;
}

export interface QueueJob {
  id: string;
  name: string;
  status: 'waiting' | 'active' | 'completed' | 'failed';
  data: Record<string, unknown>;
  failedReason?: string;
  processedOn?: string;
  finishedOn?: string;
  timestamp: string;
}

export const accountsApi = {
  list: (token: string) =>
    request<{ data: InstagramAccount[] }>('/instagram/accounts', { token }),

  getOAuthUrl: (token: string) =>
    request<{ url: string }>('/instagram/oauth/url', { token }),

  remove: (accountId: string, token: string) =>
    request<void>(`/instagram/accounts/${accountId}`, { method: 'DELETE', token }),
};

// ── Personas ─────────────────────────────────────────────────

export interface Persona {
  id: string;
  accountId: string;
  systemPrompt: string;
  delayMin: number;
  delayMax: number;
  updatedAt: string;
}

export const personasApi = {
  getByAccount: (accountId: string, token: string) =>
    request<{ data: Persona }>(`/personas/${accountId}`, { token }),

  update: (accountId: string, token: string, body: { systemPrompt: string; delayMin: number; delayMax: number }) =>
    request<{ data: Persona }>(`/personas/${accountId}`, { method: 'PUT', token, body }),
};

// ── Playground ───────────────────────────────────────────────

export interface PlaygroundResponse {
  response: string;
  tokensUsed: number;
  latencyMs: number;
}

export const playgroundApi = {
  test: (token: string, body: { accountId: string; message: string }) =>
    request<{ data: PlaygroundResponse }>('/playground/test', { method: 'POST', token, body }),
};

// ── Logs ─────────────────────────────────────────────────────

export interface ProcessingLog {
  id: string;
  accountId: string;
  accountUsername: string;
  conversationId: string;
  status: 'success' | 'error' | 'timeout';
  errorMessage?: string;
  processingTimeMs: number;
  createdAt: string;
}

export const logsApi = {
  list: (token: string, params?: { accountId?: string; status?: string; startDate?: string; endDate?: string; page?: number; pageSize?: number }) => {
    const qs = new URLSearchParams();
    if (params?.accountId) qs.set('accountId', params.accountId);
    if (params?.status) qs.set('status', params.status);
    if (params?.startDate) qs.set('startDate', params.startDate);
    if (params?.endDate) qs.set('endDate', params.endDate);
    if (params?.page !== undefined) qs.set('page', String(params.page));
    if (params?.pageSize !== undefined) qs.set('pageSize', String(params.pageSize));
    const query = qs.toString() ? `?${qs.toString()}` : '';
    return request<{ data: ProcessingLog[]; total: number }>(`/logs${query}`, { token });
  },
};
```

IMPORTANTE: Manter a ordem exata das interfaces ANTES dos objetos API que as usam. `QueueJob` deve ser declarado antes de `queueApi.getJobs` que o referencia.
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npm run lint 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contem "lucide-react" e "recharts" em dependencies
    - `api.ts` exporta queueApi, accountsApi, personasApi, playgroundApi, logsApi
    - `api.ts` exporta interfaces: QueueStats, QueueJob, InstagramAccount, Persona, PlaygroundResponse, ProcessingLog
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>lucide-react e recharts instalados; api.ts completo com todos os endpoints para M5 e M6</done>
</task>

<task type="auto">
  <name>Task 2: Criar componentes UI compartilhados (MetricCard, StatusCard, Skeleton, Banner, EmptyState)</name>
  <files>
    frontend/components/MetricCard.tsx,
    frontend/components/StatusCard.tsx,
    frontend/components/Skeleton.tsx,
    frontend/components/Banner.tsx,
    frontend/components/EmptyState.tsx,
    frontend/app/globals.css
  </files>
  <read_first>
    - frontend/app/globals.css (CSS variables e estilos existentes)
    - frontend/app/(client)/dashboard/page.tsx (MetricCard inline existente — extrair e melhorar)
    - frontend/app/(admin)/dashboard/page.tsx (StatusCard inline existente — extrair e melhorar)
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (design contract completo)
  </read_first>
  <action>
Criar diretorio `frontend/components/` e os 5 componentes abaixo. TODOS sao Server Components (sem 'use client') exceto Banner.

**1. `frontend/components/MetricCard.tsx`** — Server Component
```typescript
interface MetricCardProps {
  title: string;      // Label typography (12px, 400)
  value: string;      // Display typography (28px, 700)
  description?: string; // Body typography (14px, 400), muted
}
```
- Background: `var(--color-surface)`
- Border: `1px solid var(--color-border)`
- Border-radius: `var(--radius)`
- Padding: `24px` (lg)
- Box-shadow: `var(--shadow)`
- Title: `fontSize: '0.75rem'`, `fontWeight: 400`, `color: var(--color-text-muted)`, `marginBottom: 8px`
- Value: `fontSize: '1.75rem'`, `fontWeight: 700`, `color: var(--color-text)`
- Description (se houver): `fontSize: '0.875rem'`, `fontWeight: 400`, `color: var(--color-text-muted)`, `marginTop: 4px`

**2. `frontend/components/StatusCard.tsx`** — Server Component
```typescript
interface StatusCardProps {
  title: string;
  value: string;
  description: string;
  borderColor: string;  // CSS variable string, ex: 'var(--color-primary)'
}
```
- Mesmo que MetricCard MAS com `borderLeft: 4px solid ${borderColor}`
- Padding: `24px` (lg)

**3. `frontend/components/Skeleton.tsx`** — Server Component
```typescript
interface SkeletonProps {
  width?: string;   // default '100%'
  height?: string;  // default '20px'
  borderRadius?: string; // default 'var(--radius)'
}
```
- Background: `var(--color-border)` com opacity 0.5
- Animacao CSS: pulsante (opacity 0.5 -> 1 -> 0.5, 1.5s infinite)
- Adicionar `@keyframes skeleton-pulse` em globals.css:
```css
@keyframes skeleton-pulse {
  0%, 100% { opacity: 0.5; }
  50% { opacity: 1; }
}
```

**4. `frontend/components/Banner.tsx`** — Client Component ('use client')
```typescript
interface BannerProps {
  type: 'success' | 'error';
  message: string;
  duration?: number;    // ms, default 4000 para success, undefined para error (persiste)
  onClose?: () => void;
}
```
- Success: background `var(--color-success)` com opacity 0.15, border-left 4px solid `var(--color-success)`, texto `var(--color-success)`
- Error: background `var(--color-error)` com opacity 0.15, border-left 4px solid `var(--color-error)`, texto `var(--color-error)`
- Padding: `16px` (md)
- Border-radius: `var(--radius)`
- Botao X para fechar (sempre visivel)
- `role="alert"` para acessibilidade
- Auto-dismiss: se type=success, chamar onClose apos duration ms via setTimeout no useEffect

**5. `frontend/components/EmptyState.tsx`** — Server Component
```typescript
interface EmptyStateProps {
  icon?: ReactNode;      // lucide-react icon element
  heading: string;       // Heading typography (18px, 700)
  body: string;          // Body typography (14px, 400), muted
}
```
- Container: display flex, flex-direction column, align-items center, padding `48px` (2xl), text-align center
- Icone: 48px, color `var(--color-text-muted)`, marginBottom `16px`
- Heading: `fontSize: '1.1rem'`, `fontWeight: 700`, `color: var(--color-text)`
- Body: `fontSize: '0.875rem'`, `fontWeight: 400`, `color: var(--color-text-muted)`, `marginTop: 8px`, max-width 400px

Todos os componentes devem exportar com `export function ComponentName` (named export, nao default).
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npm run lint 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `frontend/components/MetricCard.tsx` existe e exporta `MetricCard`
    - Arquivo `frontend/components/StatusCard.tsx` existe e exporta `StatusCard`
    - Arquivo `frontend/components/Skeleton.tsx` existe e exporta `Skeleton`
    - Arquivo `frontend/components/Banner.tsx` existe, tem `'use client'` na primeira linha, e exporta `Banner`
    - Arquivo `frontend/components/EmptyState.tsx` existe e exporta `EmptyState`
    - `globals.css` contem `@keyframes skeleton-pulse`
    - Nenhum componente usa valores hex hardcoded — apenas `var(--color-*)`
    - Zero erros TypeScript no projeto
  </acceptance_criteria>
  <done>5 componentes compartilhados criados, tipados, usando CSS variables, prontos para uso nos planos 02-05</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` retorna 0 erros
- `ls frontend/components/` mostra 5 arquivos .tsx
- `grep -c "lucide-react" frontend/package.json` retorna 1
- `grep -c "recharts" frontend/package.json` retorna 1
- `grep -c "export const queueApi" frontend/lib/api.ts` retorna 1
- `grep -c "export const logsApi" frontend/lib/api.ts` retorna 1
</verification>

<success_criteria>
- lucide-react e recharts em package.json dependencies
- 5 componentes compartilhados em frontend/components/ com tipos corretos
- api.ts com 7 API namespaces (analytics, health, queue, accounts, personas, playground, logs)
- Zero erros TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-paineis/04-01-SUMMARY.md`
</output>
