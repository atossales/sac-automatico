---
phase: 04-frontend-paineis
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - frontend/app/(admin)/accounts/page.tsx
  - frontend/components/AccountList.tsx
  - frontend/components/ConfirmModal.tsx
  - frontend/app/(admin)/personas/page.tsx
  - frontend/components/PersonaForm.tsx
autonomous: true
requirements: [FE-02, FE-05, FE-06]

must_haves:
  truths:
    - "Gestor ve lista de contas Instagram com status de token"
    - "Gestor pode iniciar fluxo OAuth para adicionar conta"
    - "Gestor pode remover conta com confirmacao modal"
    - "Gestor pode configurar persona por conta (system prompt, delay min/max)"
    - "Salvar persona mostra feedback de sucesso/erro"
    - "Autenticacao JWT e out-of-scope desta fase — paginas funcionam com token vazio (desenvolvimento)"
  artifacts:
    - path: "frontend/app/(admin)/accounts/page.tsx"
      provides: "Pagina de gestao de contas Instagram"
      min_lines: 40
    - path: "frontend/components/AccountList.tsx"
      provides: "Lista de contas com status e acoes"
      exports: ["AccountList"]
    - path: "frontend/components/ConfirmModal.tsx"
      provides: "Modal de confirmacao reutilizavel"
      exports: ["ConfirmModal"]
    - path: "frontend/app/(admin)/personas/page.tsx"
      provides: "Pagina de configuracao de personas"
      min_lines: 40
    - path: "frontend/components/PersonaForm.tsx"
      provides: "Formulario de persona por conta"
      exports: ["PersonaForm"]
  key_links:
    - from: "frontend/components/AccountList.tsx"
      to: "frontend/lib/api.ts"
      via: "accountsApi.list, accountsApi.getOAuthUrl, accountsApi.remove"
      pattern: "accountsApi\\."
    - from: "frontend/components/PersonaForm.tsx"
      to: "frontend/lib/api.ts"
      via: "personasApi.getByAccount, personasApi.update"
      pattern: "personasApi\\."
---

<objective>
Implementar telas de gestao de contas Instagram (listar, adicionar via OAuth, remover) e configuracao de personas por conta (system prompt, delay).

Purpose: M6 — gestor consegue adicionar conta e configurar persona em menos de 5 minutos.
Output: Paginas /admin/accounts e /admin/personas funcionais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-paineis/04-UI-SPEC.md
@frontend/lib/api.ts
@frontend/components/Banner.tsx
@frontend/components/EmptyState.tsx
@frontend/components/Skeleton.tsx
@frontend/app/globals.css
</context>

<interfaces>
<!-- Tipos do api.ts que este plano consome -->

```typescript
export interface InstagramAccount {
  id: string;
  igUserId: string;
  username: string;
  tokenStatus: 'active' | 'expiring' | 'expired';
  tokenExpiresAt: string;
  createdAt: string;
}

export const accountsApi = {
  list: (token: string) => request<{ data: InstagramAccount[] }>('/instagram/accounts', { token }),
  getOAuthUrl: (token: string) => request<{ url: string }>('/instagram/oauth/url', { token }),
  remove: (accountId: string, token: string) => request<void>(`/instagram/accounts/${accountId}`, { method: 'DELETE', token }),
};

export interface Persona {
  id: string;
  accountId: string;
  systemPrompt: string;
  delayMin: number;
  delayMax: number;
  updatedAt: string;
}

export const personasApi = {
  getByAccount: (accountId: string, token: string) => request<{ data: Persona }>(`/personas/${accountId}`, { token }),
  update: (accountId: string, token: string, body: { systemPrompt: string; delayMin: number; delayMax: number }) => request<{ data: Persona }>(`/personas/${accountId}`, { method: 'PUT', token, body }),
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar AccountList, ConfirmModal e pagina de contas</name>
  <files>
    frontend/components/AccountList.tsx,
    frontend/components/ConfirmModal.tsx,
    frontend/app/(admin)/accounts/page.tsx
  </files>
  <read_first>
    - frontend/lib/api.ts (accountsApi, InstagramAccount)
    - frontend/components/Banner.tsx (para feedback de sucesso/erro)
    - frontend/components/EmptyState.tsx (para estado sem contas)
    - frontend/components/Skeleton.tsx (para loading)
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (Copywriting M6, Interacoes — OAuth, remocao)
  </read_first>
  <action>
**1. `frontend/components/ConfirmModal.tsx`** — Client Component ('use client')

```typescript
interface ConfirmModalProps {
  isOpen: boolean;
  title: string;
  message: string;
  confirmLabel: string;     // ex: "Sim, remover"
  cancelLabel?: string;     // default "Cancelar"
  confirmVariant?: 'danger' | 'primary';  // default 'danger'
  loading?: boolean;
  onConfirm: () => void;
  onCancel: () => void;
}
export function ConfirmModal(props: ConfirmModalProps): JSX.Element | null
```

- Se `!isOpen`, retornar null
- Backdrop: position fixed, inset 0, background rgba(0,0,0,0.6), z-index 100, display flex, align-items center, justify-content center
- Modal: background `var(--color-surface)`, border `1px solid var(--color-border)`, border-radius `var(--radius)`, padding `24px`, max-width 480px, width `calc(100% - 32px)`, box-shadow `var(--shadow)`
- Title: Heading (1.1rem, 700), marginBottom 8px
- Message: Body (0.875rem, 400, muted), marginBottom 24px
- Botoes: div flex, gap 8px, justify-content flex-end
  - Cancelar: background transparent, border `1px solid var(--color-border)`, color `var(--color-text)`, padding `8px 16px`, border-radius `var(--radius)`, cursor pointer
  - Confirmar danger: background `var(--color-error)`, color white, border none, padding `8px 16px`, border-radius `var(--radius)`, cursor pointer
  - Confirmar primary: background `var(--color-primary)`, color white
  - Loading: disabled, opacity 0.7, cursor not-allowed
- `role="dialog"`, `aria-modal="true"`, `aria-labelledby` vinculado ao titulo
- Fechar ao clicar no backdrop (chamar onCancel)
- Fechar ao pressionar Escape (useEffect com keydown listener)

**2. `frontend/components/AccountList.tsx`** — Client Component ('use client')

```typescript
interface AccountListProps {
  token: string;
}
export function AccountList({ token }: AccountListProps): JSX.Element
```

**Estado:**
```typescript
const [accounts, setAccounts] = useState<InstagramAccount[]>([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState<string | null>(null);
const [removingId, setRemovingId] = useState<string | null>(null);  // ID da conta no modal
const [removeLoading, setRemoveLoading] = useState(false);
const [banner, setBanner] = useState<{ type: 'success' | 'error'; message: string } | null>(null);
```

**Fetch:** useEffect chama `accountsApi.list(token)` no mount.

**Botao CTA "Conectar conta Instagram":**
- Posicao: canto superior direito do header (flex, justify-content space-between)
- Background `var(--color-primary)`, color white, padding `8px 16px`, border-radius `var(--radius)`, fontWeight 700, fontSize 0.875rem, border none, cursor pointer, min-height 44px
- Hover: background `var(--color-primary-hover)`
- Importar icone `Plus` de lucide-react, size 16, inline antes do texto, gap 4px
- onClick: chamar `accountsApi.getOAuthUrl(token)` e redirecionar com `window.location.href = url` (janela atual, nao popup — conforme UI-SPEC)

**Lista de contas:**
Cada conta em um card (nao tabela):
- Background: `var(--color-surface)`, border, border-radius, padding 24px
- Layout: flex, justify-content space-between, align-items center
- Esquerda:
  - Username: `@{username}` (Body, 0.875rem, 700 neste caso por ser titulo do item)
  - Status badge: span inline
    - active: background `var(--color-success)` opacity 0.15, color `var(--color-success)`, texto "Token ativo"
    - expiring: background `var(--color-warning)` opacity 0.15, color `var(--color-warning)`, texto "Expira em {N} dias" (calcular dias com `Math.ceil((new Date(tokenExpiresAt).getTime() - Date.now()) / 86400000)`)
    - expired: background `var(--color-error)` opacity 0.15, color `var(--color-error)`, texto "Token expirado"
  - Badge: padding `2px 8px`, border-radius 999px, fontSize 0.75rem, marginLeft 8px
- Direita: botao "Remover conta"
  - Background transparent, border `1px solid var(--color-error)`, color `var(--color-error)`, padding `8px 16px`, border-radius `var(--radius)`, fontSize 0.875rem, cursor pointer, min-height 44px
  - Hover: background `var(--color-error)` opacity 0.1
  - onClick: `setRemovingId(account.id)` — abre modal

**ConfirmModal:**
- isOpen: `removingId !== null`
- title: "Remover conta"
- message: "Tem certeza que deseja remover esta conta? Esta acao desconecta a conta Instagram e apaga a persona configurada. Esta acao nao pode ser desfeita."
- confirmLabel: "Sim, remover"
- cancelLabel: "Cancelar"
- confirmVariant: "danger"
- loading: `removeLoading`
- onConfirm: chamar `accountsApi.remove(removingId!, token)`, em sucesso: remover da lista local, setBanner success "Conta removida com sucesso", setRemovingId(null). Em erro: setBanner error com mensagem.
- onCancel: `setRemovingId(null)`

**Empty state:** EmptyState com heading "Nenhuma conta conectada" e body "Clique em 'Conectar conta Instagram' para adicionar a primeira conta Business." Importar icone `Instagram` de lucide-react para o icon prop.

**Banner:** Renderizar Banner no topo da lista quando `banner !== null`. onClose: `setBanner(null)`.

**Loading:** 3 Skeleton cards (height 80px, marginBottom 16px).

**3. `frontend/app/(admin)/accounts/page.tsx`** — Client Component ('use client')

Pagina simples que renderiza o AccountList:
```tsx
'use client';

import { AccountList } from '@/components/AccountList';

export default function AccountsPage(): JSX.Element {
  // TODO: obter token de autenticacao
  const token = '';

  return (
    <div>
      <header style={{ marginBottom: '32px' }}>
        <h1 style={{ fontSize: '1.75rem', fontWeight: 700, color: 'var(--color-text)' }}>
          Contas Instagram
        </h1>
        <p style={{ color: 'var(--color-text-muted)', marginTop: '4px', fontSize: '0.875rem' }}>
          Gerencie as contas Business conectadas ao SAC Automatico
        </p>
      </header>
      <AccountList token={token} />
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npm run lint 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `ConfirmModal.tsx` tem `role="dialog"` e `aria-modal="true"`, usa `useEffect` para Escape key
    - `AccountList.tsx` importa `accountsApi` de `@/lib/api`, importa `Plus` e `Instagram` de `lucide-react`
    - `AccountList.tsx` contem texto "Conectar conta Instagram" e "Sim, remover"
    - `AccountList.tsx` renderiza ConfirmModal com message contendo "Esta acao nao pode ser desfeita"
    - `accounts/page.tsx` existe em `frontend/app/(admin)/accounts/`
    - Token status badges usam 3 estados: active/expiring/expired
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>Pagina de contas com lista, adicao via OAuth, remocao com confirmacao modal</done>
</task>

<task type="auto">
  <name>Task 2: Criar PersonaForm e pagina de personas</name>
  <files>
    frontend/components/PersonaForm.tsx,
    frontend/app/(admin)/personas/page.tsx
  </files>
  <read_first>
    - frontend/lib/api.ts (personasApi, accountsApi, Persona, InstagramAccount)
    - frontend/components/Banner.tsx (para feedback)
    - frontend/components/Skeleton.tsx (para loading)
    - frontend/components/EmptyState.tsx (para sem contas)
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (Copywriting M6 — personas, Interacoes — salvar persona)
  </read_first>
  <action>
**1. `frontend/components/PersonaForm.tsx`** — Client Component ('use client')

```typescript
interface PersonaFormProps {
  accountId: string;
  accountUsername: string;
  token: string;
}
export function PersonaForm({ accountId, accountUsername, token }: PersonaFormProps): JSX.Element
```

**Estado:**
```typescript
const [systemPrompt, setSystemPrompt] = useState('');
const [delayMin, setDelayMin] = useState(3);
const [delayMax, setDelayMax] = useState(12);
const [loading, setLoading] = useState(true);
const [saving, setSaving] = useState(false);
const [banner, setBanner] = useState<{ type: 'success' | 'error'; message: string } | null>(null);
```

**Fetch inicial:** useEffect chama `personasApi.getByAccount(accountId, token)`. Se sucesso, preencher form com valores existentes. Se 404, manter defaults. Se outro erro, mostrar banner error.

**Layout do formulario:**
- Container: background `var(--color-surface)`, border, border-radius, padding 24px
- Header do card: username `@{accountUsername}` (Body, 0.875rem, 700), marginBottom 24px

**Campo System Prompt:**
- `<label htmlFor="system-prompt-{accountId}">` com texto "System Prompt" (Label, 0.75rem, 400, muted)
- `<textarea id="system-prompt-{accountId}">` com:
  - width 100%, min-height 200px, resize vertical
  - background `var(--color-bg)`, border `1px solid var(--color-border)`, border-radius `var(--radius)`
  - padding 16px, fontSize 0.875rem, color `var(--color-text)`, fontFamily inherit
  - Focus: border-color `var(--color-primary)`, outline `2px solid var(--color-primary)`, outline-offset 2px
  - placeholder: "Descreva a personalidade e tom de voz do atendente..."
  - value={systemPrompt}, onChange={e => setSystemPrompt(e.target.value)}

**Campo Delay Minimo:**
- `<label htmlFor="delay-min-{accountId}">` com texto "Delay minimo (segundos)" (Label)
- `<input type="range" id="delay-min-{accountId}">` min=1, max=10, step=1, value={delayMin}
- Span com valor atual: "{delayMin}s" (Body, 0.875rem)
- Range input style: width 100%, accent-color `var(--color-primary)`
- onChange: setDelayMin, mas garantir delayMin <= delayMax. Se delayMin > delayMax, ajustar delayMax = delayMin.

**Campo Delay Maximo:**
- `<label htmlFor="delay-max-{accountId}">` com texto "Delay maximo (segundos)" (Label)
- `<input type="range" id="delay-max-{accountId}">` min=1, max=30, step=1, value={delayMax}
- Span com valor atual: "{delayMax}s"
- onChange: setDelayMax, mas garantir delayMax >= delayMin. Se delayMax < delayMin, ajustar delayMin = delayMax.

**Botao Salvar:**
- Texto: "Salvar persona" (normal) / "Salvando..." (durante request)
- Background `var(--color-primary)`, color white, padding `8px 16px`, border-radius `var(--radius)`, fontWeight 700, fontSize 0.875rem, border none, cursor pointer, min-height 44px
- Hover: background `var(--color-primary-hover)`
- Disabled durante saving: opacity 0.7, cursor not-allowed
- onClick: chamar `personasApi.update(accountId, token, { systemPrompt, delayMin, delayMax })`. Sucesso: setBanner success "Persona salva com sucesso". Erro: setBanner error com mensagem.

**Banner:** Renderizar Banner acima do form quando `banner !== null`.

**Loading:** Skeleton textarea (height 200px) + 2 Skeleton ranges (height 40px).

**2. `frontend/app/(admin)/personas/page.tsx`** — Client Component ('use client')

```typescript
export default function PersonasPage(): JSX.Element
```

**Estado:**
```typescript
const [accounts, setAccounts] = useState<InstagramAccount[]>([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState<string | null>(null);
```

**Fetch:** useEffect chama `accountsApi.list(token)` no mount.

**Layout:**
```
header (marginBottom 32px)
  h1 "Personas" (Display: 1.75rem, 700)
  p "Configure a personalidade da IA para cada conta Instagram" (Body, 0.875rem, 400, muted, marginTop 4px)

Se loading: 2 Skeleton cards (height 400px)
Se error: banner de erro com retry
Se accounts.length === 0: EmptyState heading "Nenhuma conta conectada", body "Adicione uma conta Instagram na pagina Contas para configurar personas."
Se accounts: lista vertical de PersonaForm, um por conta, gap 24px entre eles
```

Token: string vazia '' por enquanto. Comentario `// TODO: obter token de autenticacao`.
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npm run lint 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `PersonaForm.tsx` tem `'use client'`, importa `personasApi` de `@/lib/api`
    - `PersonaForm.tsx` contem `<textarea` e dois `<input type="range"`
    - `PersonaForm.tsx` contem `<label htmlFor` para cada input (acessibilidade)
    - `PersonaForm.tsx` contem texto "Salvar persona" e "Salvando..."
    - `personas/page.tsx` existe em `frontend/app/(admin)/personas/`
    - `personas/page.tsx` renderiza PersonaForm para cada conta
    - Delay range valida min <= max
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>Paginas de contas e personas funcionais — gestor pode gerenciar contas e configurar personas</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` retorna 0 erros
- `ls frontend/app/\(admin\)/accounts/page.tsx` existe
- `ls frontend/app/\(admin\)/personas/page.tsx` existe
- `grep -r "accountsApi" frontend/components/AccountList.tsx` encontra match
- `grep -r "personasApi" frontend/components/PersonaForm.tsx` encontra match
- `grep -r "role=\"dialog\"" frontend/components/ConfirmModal.tsx` encontra match
</verification>

<success_criteria>
- Pagina /admin/accounts lista contas com status de token (ativo/expirando/expirado)
- Botao "Conectar conta Instagram" inicia fluxo OAuth
- Remocao de conta pede confirmacao via modal
- Pagina /admin/personas mostra um formulario por conta
- Salvar persona mostra feedback de sucesso/erro via Banner
- Delay sliders validam min <= max
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-paineis/04-04-SUMMARY.md`
</output>
