---
phase: 04-frontend-paineis
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/app/(client)/dashboard/page.tsx
  - frontend/components/AccountFilter.tsx
  - frontend/components/ClickChart.tsx
  - frontend/components/ConversationTable.tsx
autonomous: true
requirements: [FE-01, FE-05, FE-06]

must_haves:
  truths:
    - "Cliente ve metricas agregadas (conversas, mensagens, cliques, contas ativas)"
    - "Cliente filtra dados por conta Instagram"
    - "Cliente ve grafico de cliques por dia com toggle 7/30 dias"
    - "Cliente ve lista de conversas paginada com 20 itens por pagina"
    - "Todos os estados (loading, empty, error, populated) sao tratados"
    - "Autenticacao JWT e out-of-scope desta fase — paginas funcionam com token vazio (desenvolvimento)"
  artifacts:
    - path: "frontend/app/(client)/dashboard/page.tsx"
      provides: "Pagina principal do painel do cliente"
      min_lines: 80
    - path: "frontend/components/AccountFilter.tsx"
      provides: "Seletor de conta Instagram"
      exports: ["AccountFilter"]
    - path: "frontend/components/ClickChart.tsx"
      provides: "Grafico de serie temporal com recharts"
      exports: ["ClickChart"]
    - path: "frontend/components/ConversationTable.tsx"
      provides: "Tabela paginada de conversas"
      exports: ["ConversationTable"]
  key_links:
    - from: "frontend/app/(client)/dashboard/page.tsx"
      to: "frontend/lib/api.ts"
      via: "analyticsApi.getAllSummary, getAccountSummary"
      pattern: "analyticsApi\\."
    - from: "frontend/components/ClickChart.tsx"
      to: "frontend/lib/api.ts"
      via: "analyticsApi.getClickTimeSeries"
      pattern: "analyticsApi\\.getClickTimeSeries"
    - from: "frontend/components/ConversationTable.tsx"
      to: "frontend/lib/api.ts"
      via: "analyticsApi.getConversations"
      pattern: "analyticsApi\\.getConversations"
---

<objective>
Implementar o painel completo do cliente (M5) com dashboard de metricas, grafico de cliques, filtro por conta e lista de conversas paginada.

Purpose: Entrega do M5 — cliente consegue acompanhar performance do SAC sem configuracao manual.
Output: Pagina funcional em /client/dashboard com 3 componentes interativos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-paineis/04-UI-SPEC.md
@frontend/lib/api.ts
@frontend/app/(client)/layout.tsx
@frontend/app/(client)/dashboard/page.tsx
@frontend/components/MetricCard.tsx
@frontend/components/EmptyState.tsx
@frontend/components/Skeleton.tsx
@frontend/app/globals.css
</context>

<interfaces>
<!-- Tipos ja existentes em frontend/lib/api.ts que este plano consome -->

```typescript
export interface AccountSummary {
  accountId: string;
  accountName: string;
  totalConversations: number;
  totalMessages: number;
  totalLinks: number;
  totalClicks: number;
}

export interface ConversationStats {
  conversationId: string;
  participantId: string;
  participantUsername: string | null;
  messageCount: number;
  lastMessageAt: string;
}

export interface ClickTimeSeries {
  date: string;
  clicks: number;
}

export const analyticsApi = {
  getAllSummary: (token: string) => request<{ data: AccountSummary[] }>(...),
  getAccountSummary: (accountId: string, token: string) => request<{ data: AccountSummary }>(...),
  getConversations: (accountId: string, token: string, params?) => request<{ data: ConversationStats[]; total: number }>(...),
  getClickTimeSeries: (accountId: string, token: string, days?: number) => request<{ data: ClickTimeSeries[] }>(...),
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar componentes AccountFilter, ClickChart e ConversationTable</name>
  <files>
    frontend/components/AccountFilter.tsx,
    frontend/components/ClickChart.tsx,
    frontend/components/ConversationTable.tsx
  </files>
  <read_first>
    - frontend/lib/api.ts (tipos AccountSummary, ClickTimeSeries, ConversationStats)
    - frontend/components/EmptyState.tsx (para reusar em empty states)
    - frontend/components/Skeleton.tsx (para loading states)
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (secoes Componentes, Copywriting M5, Responsividade)
  </read_first>
  <action>
**1. `frontend/components/AccountFilter.tsx`** — Client Component ('use client')

```typescript
interface AccountFilterProps {
  accounts: Array<{ accountId: string; accountName: string }>;
  selectedAccountId: string | null;
  onSelect: (accountId: string) => void;
}
```
- Renderizar grupo de botoes (nao select nativo) com display flex, gap 8px, flex-wrap wrap
- Cada botao: padding `8px 16px`, border-radius `var(--radius)`, fontSize `0.875rem`, cursor pointer
- Botao selecionado: background `var(--color-primary)`, color white, border none
- Botao nao selecionado: background transparent, border `1px solid var(--color-border)`, color `var(--color-text-muted)`
- Hover nao selecionado: border-color `var(--color-text-muted)`
- Cada botao tem `aria-pressed={selected}` para acessibilidade
- Min-height 44px para touch targets mobile

**2. `frontend/components/ClickChart.tsx`** — Client Component ('use client')

```typescript
interface ClickChartProps {
  accountId: string;
  token: string;
}
```
- Gerencia proprio state: `days` (7 | 30, default 7), `data` (ClickTimeSeries[]), `loading`, `error`
- useEffect fetch: chama `analyticsApi.getClickTimeSeries(accountId, token, days)` quando accountId ou days muda
- Titulo: h2 "Cliques por dia" (Heading, 18px/700)
- Toggle 7/30 dias: dois botoes inline, mesmo estilo do AccountFilter (selecionado = primary)
- Texto dos toggles: "Ultimos 7 dias" e "Ultimos 30 dias" (conforme copywriting contract)
- Grafico: recharts `AreaChart` dentro de `ResponsiveContainer` width="100%" height={240}
  - Area com fill `var(--color-primary)` opacity 0.2, stroke `var(--color-primary)` strokeWidth 2
  - XAxis dataKey="date" com tick fontSize 12, fill `var(--color-text-muted)`
  - YAxis com tick fontSize 12, fill `var(--color-text-muted)`
  - Tooltip com background `var(--color-surface)`, border `1px solid var(--color-border)`
  - CartesianGrid com stroke `var(--color-border)` opacity 0.3
- Container: background `var(--color-surface)`, border, border-radius, padding 24px
- `aria-label="Grafico de cliques por dia"` na div container
- Loading: Skeleton com height 240px
- Error: mensagem com `var(--color-error)`
- Empty: texto "Nenhum dado de cliques para o periodo selecionado"

**3. `frontend/components/ConversationTable.tsx`** — Client Component ('use client')

```typescript
interface ConversationTableProps {
  accountId: string;
  token: string;
}
```
- Gerencia proprio state: `page` (default 1), `data` (ConversationStats[]), `total`, `loading`, `error`
- pageSize fixo = 20
- useEffect fetch: chama `analyticsApi.getConversations(accountId, token, { page, pageSize: 20 })` quando accountId ou page muda
- Titulo: h2 "Conversas recentes" (Heading, 18px/700), marginBottom 16px
- Tabela semantica: `<table>` com width 100%, border-collapse collapse
- `<thead>` com `<th scope="col">` para cada coluna: "Participante", "Mensagens", "Ultima atividade"
- th: fontSize 0.75rem, fontWeight 700, color `var(--color-text-muted)`, padding `8px 16px`, text-align left, border-bottom `1px solid var(--color-border)`
- td: fontSize 0.875rem, padding `8px 16px`, border-bottom `1px solid var(--color-border)`, color `var(--color-text)`
- participantUsername: mostrar `@{username}` ou "Desconhecido" se null
- lastMessageAt: formatar como "DD/MM/YYYY HH:mm" usando Intl.DateTimeFormat('pt-BR')
- Paginacao: div com display flex, justify-content space-between, marginTop 16px
  - Botao "Anterior" (disabled se page === 1)
  - Texto "Pagina {page} de {totalPages}"
  - Botao "Proxima" (disabled se page >= totalPages)
  - Botoes: padding `8px 16px`, border `1px solid var(--color-border)`, background transparent, color `var(--color-text)`, border-radius `var(--radius)`, cursor pointer
  - Disabled: opacity 0.5, cursor not-allowed
- Container: background `var(--color-surface)`, border, border-radius, padding 24px
- Overflow-x: auto para mobile (tabela scrollavel horizontalmente)
- Loading: 5 linhas de Skeleton
- Empty: EmptyState com heading "Nenhuma conversa ainda" e body "As conversas do Instagram aparecerao aqui assim que o sistema receber a primeira mensagem."
- Error: mensagem vermelha com botao "Tentar novamente"
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npm run lint 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `AccountFilter.tsx` tem `'use client'` na linha 1 e exporta `AccountFilter`
    - `ClickChart.tsx` tem `'use client'`, importa de `recharts` (AreaChart, ResponsiveContainer), importa `analyticsApi` de `@/lib/api`
    - `ConversationTable.tsx` tem `'use client'`, usa `<table>` com `<thead>` e `scope="col"`, importa `analyticsApi` de `@/lib/api`
    - Todos usam `var(--color-*)` para cores, nenhum hex hardcoded
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>3 componentes interativos do M5 criados com loading/empty/error/populated states</done>
</task>

<task type="auto">
  <name>Task 2: Montar pagina do dashboard do cliente com todos os componentes</name>
  <files>frontend/app/(client)/dashboard/page.tsx</files>
  <read_first>
    - frontend/app/(client)/dashboard/page.tsx (placeholder atual — sera substituido)
    - frontend/app/(client)/layout.tsx (entender estrutura do layout)
    - frontend/components/MetricCard.tsx (interface MetricCardProps)
    - frontend/components/AccountFilter.tsx (interface AccountFilterProps)
    - frontend/components/ClickChart.tsx (interface ClickChartProps)
    - frontend/components/ConversationTable.tsx (interface ConversationTableProps)
    - frontend/lib/api.ts (analyticsApi.getAllSummary)
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (secao M5 Copywriting + Layout)
  </read_first>
  <action>
Reescrever `frontend/app/(client)/dashboard/page.tsx` completamente. Deve ser um Client Component ('use client') pois gerencia estado de conta selecionada e faz fetch de dados.

**Estrutura da pagina:**

```
header (marginBottom 32px)
  h1 "Seus Relatorios" (Display: 1.75rem, 700)
  p "Acompanhe o desempenho do atendimento automatico" (Body: 0.875rem, 400, muted, marginTop 4px)

AccountFilter (marginBottom 24px)
  - Buscar contas via analyticsApi.getAllSummary(token) no useEffect inicial
  - Passar accounts como array de { accountId, accountName }
  - Ao selecionar, setar selectedAccountId no state

MetricCards grid (marginBottom 32px)
  - grid: repeat(auto-fill, minmax(240px, 1fr)), gap 24px
  - Se nenhuma conta selecionada: mostrar metricas agregadas (somar todos os AccountSummary)
  - Se conta selecionada: mostrar metricas daquela conta
  - 4 cards:
    1. MetricCard title="Conversas Atendidas" value={totalConversations}
    2. MetricCard title="Mensagens Respondidas" value={totalMessages}
    3. MetricCard title="Cliques em Links" value={totalClicks}
    4. MetricCard title="Contas Ativas" value={accounts.length} (sempre agregado)
  - Loading: 4 Skeleton cards (height 120px)

ClickChart (marginBottom 32px)
  - Renderizar apenas quando selectedAccountId existe
  - Se nenhuma conta selecionada: EmptyState com heading "Selecione uma conta acima para ver os dados"
  - Passar accountId e token

ConversationTable
  - Renderizar apenas quando selectedAccountId existe
  - Se nenhuma conta selecionada: nao renderizar (ClickChart ja mostra a mensagem)
  - Passar accountId e token
```

**Estado:**
```typescript
const [accounts, setAccounts] = useState<AccountSummary[]>([]);
const [selectedAccountId, setSelectedAccountId] = useState<string | null>(null);
const [loading, setLoading] = useState(true);
const [error, setError] = useState<string | null>(null);
```

**Token:** Por enquanto, usar string vazia '' como token (auth sera implementada depois). Adicionar comentario `// TODO: obter token de autenticacao` no topo do componente.

**Tratamento de erro global da pagina:**
- Se fetch de contas falhar: mostrar div com role="alert", background `var(--color-error)` opacity 0.15, texto "Nao foi possivel carregar os dados. Verifique a conexao e tente novamente." com botao "Tentar novamente" que re-executa o fetch.
- Se erro 401: `window.location.href = '/auth/login'`

**Metadata:** Como e Client Component, NAO pode ter export const metadata. Remover o metadata existente. Titulo sera gerido pelo layout.

**Formatacao de numeros:** Usar `toLocaleString('pt-BR')` para formatar valores numericos grandes (ex: 1.234 com ponto de milhar).
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npm run lint 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` tem `'use client'` na primeira linha
    - Importa e usa MetricCard, AccountFilter, ClickChart, ConversationTable, EmptyState, Skeleton
    - Importa `analyticsApi` e `AccountSummary` de `@/lib/api`
    - Contem texto "Seus Relatorios" e "Acompanhe o desempenho do atendimento automatico"
    - Grid de metricas usa `minmax(240px, 1fr)` e gap `24px`
    - NAO contem `export const metadata`
    - Contem `role="alert"` para estado de erro
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>Dashboard do cliente (M5) completo com metricas, filtro de conta, grafico de cliques e tabela de conversas</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` retorna 0 erros
- Pagina /client/dashboard importa e renderiza AccountFilter, MetricCard, ClickChart, ConversationTable
- Todos os textos do copywriting contract M5 presentes
- Grid usa 240px minmax (nao 280px) e gap 24px (nao 20px) conforme UI-SPEC
</verification>

<success_criteria>
- Painel do cliente (M5) funcional em /client/dashboard
- 4 metricas visiveis, filtro por conta, grafico de cliques com toggle 7/30 dias, tabela paginada
- Loading, empty, error states implementados em todos os componentes
- Mobile responsive (grid 1 col em 320px, tabela scrollavel)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-paineis/04-02-SUMMARY.md`
</output>
