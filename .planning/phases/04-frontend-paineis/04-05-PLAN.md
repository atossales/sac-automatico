---
phase: 04-frontend-paineis
plan: 05
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - frontend/app/(admin)/playground/page.tsx
  - frontend/components/PromptPlayground.tsx
  - frontend/app/(admin)/logs/page.tsx
  - frontend/components/LogTable.tsx
autonomous: true
requirements: [FE-04, FE-05, FE-06]

must_haves:
  truths:
    - "Gestor seleciona conta, digita mensagem e recebe resposta simulada da IA"
    - "Playground mostra aviso de que nenhum DM real sera enviado"
    - "Gestor filtra logs por conta, status e periodo"
    - "Tabela de logs mostra status com badge colorido"
    - "Autenticacao JWT e out-of-scope desta fase — paginas funcionam com token vazio (desenvolvimento)"
  artifacts:
    - path: "frontend/app/(admin)/playground/page.tsx"
      provides: "Pagina de playground de prompts"
      min_lines: 30
    - path: "frontend/components/PromptPlayground.tsx"
      provides: "Interface de teste de prompts"
      exports: ["PromptPlayground"]
    - path: "frontend/app/(admin)/logs/page.tsx"
      provides: "Pagina de logs de processamento"
      min_lines: 30
    - path: "frontend/components/LogTable.tsx"
      provides: "Tabela de logs filtrada"
      exports: ["LogTable"]
  key_links:
    - from: "frontend/components/PromptPlayground.tsx"
      to: "frontend/lib/api.ts"
      via: "playgroundApi.test"
      pattern: "playgroundApi\\.test"
    - from: "frontend/components/LogTable.tsx"
      to: "frontend/lib/api.ts"
      via: "logsApi.list"
      pattern: "logsApi\\.list"
---

<objective>
Implementar playground de prompts (testar resposta da IA sem enviar DM) e pagina de logs de processamento com filtros por conta, status e periodo.

Purpose: M6 completo — playground para testar prompts e logs para debugging.
Output: Paginas /admin/playground e /admin/logs funcionais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-paineis/04-UI-SPEC.md
@frontend/lib/api.ts
@frontend/components/Skeleton.tsx
@frontend/components/EmptyState.tsx
@frontend/app/globals.css
</context>

<interfaces>
<!-- Tipos do api.ts que este plano consome -->

```typescript
export interface PlaygroundResponse {
  response: string;
  tokensUsed: number;
  latencyMs: number;
}

export const playgroundApi = {
  test: (token: string, body: { accountId: string; message: string }) =>
    request<{ data: PlaygroundResponse }>('/playground/test', { method: 'POST', token, body }),
};

export interface ProcessingLog {
  id: string;
  accountId: string;
  accountUsername: string;
  conversationId: string;
  status: 'success' | 'error' | 'timeout';
  errorMessage?: string;
  processingTimeMs: number;
  createdAt: string;
}

export const logsApi = {
  list: (token: string, params?: { accountId?: string; status?: string; startDate?: string; endDate?: string; page?: number; pageSize?: number }) =>
    request<{ data: ProcessingLog[]; total: number }>(`/logs${query}`, { token }),
};

export interface InstagramAccount {
  id: string;
  igUserId: string;
  username: string;
  tokenStatus: 'active' | 'expiring' | 'expired';
  tokenExpiresAt: string;
  createdAt: string;
}

export const accountsApi = {
  list: (token: string) => request<{ data: InstagramAccount[] }>('/instagram/accounts', { token }),
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar PromptPlayground e pagina de playground</name>
  <files>
    frontend/components/PromptPlayground.tsx,
    frontend/app/(admin)/playground/page.tsx
  </files>
  <read_first>
    - frontend/lib/api.ts (playgroundApi, accountsApi, InstagramAccount, PlaygroundResponse)
    - frontend/components/Skeleton.tsx
    - frontend/components/EmptyState.tsx
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (Copywriting M6 — playground, Interacoes — testar prompt)
  </read_first>
  <action>
**1. `frontend/components/PromptPlayground.tsx`** — Client Component ('use client')

```typescript
interface PromptPlaygroundProps {
  token: string;
}
export function PromptPlayground({ token }: PromptPlaygroundProps): JSX.Element
```

**Estado:**
```typescript
const [accounts, setAccounts] = useState<InstagramAccount[]>([]);
const [selectedAccountId, setSelectedAccountId] = useState<string>('');
const [message, setMessage] = useState('');
const [response, setResponse] = useState<PlaygroundResponse | null>(null);
const [loading, setLoading] = useState(false);
const [accountsLoading, setAccountsLoading] = useState(true);
const [error, setError] = useState<string | null>(null);
```

**Fetch contas:** useEffect chama `accountsApi.list(token)` no mount para popular o seletor.

**Layout:**

Aviso no topo:
- div com background `var(--color-warning)` opacity 0.1, border-left 4px solid `var(--color-warning)`, padding 16px, border-radius `var(--radius)`, marginBottom 24px
- Icone `AlertTriangle` de lucide-react (size 16, inline, color `var(--color-warning)`)
- Texto: "Esta e uma simulacao. Nenhum DM real sera enviado." (Body, 0.875rem, color `var(--color-warning)`)

Seletor de conta:
- `<label htmlFor="playground-account">` com texto "Conta Instagram" (Label, 0.75rem, muted)
- `<select id="playground-account">` com option default "Selecione uma conta..." (disabled)
- Cada option: `account.username`
- Select style: width 100%, padding `8px 16px`, background `var(--color-bg)`, border `1px solid var(--color-border)`, border-radius `var(--radius)`, color `var(--color-text)`, fontSize 0.875rem, min-height 44px
- Focus: border-color `var(--color-primary)`, outline 2px solid `var(--color-primary)`, outline-offset 2px
- marginBottom 16px

Campo de mensagem:
- `<label htmlFor="playground-message">` com texto "Mensagem de teste" (Label, 0.75rem, muted)
- `<textarea id="playground-message">` com:
  - placeholder: "Digite uma mensagem como se fosse um cliente..."
  - width 100%, min-height 120px, resize vertical
  - Mesmos estilos do textarea da PersonaForm
  - value={message}, onChange
- marginBottom 16px

Botao "Testar resposta":
- Background `var(--color-primary)`, color white, padding `8px 16px`, border-radius `var(--radius)`, fontWeight 700, fontSize 0.875rem, border none, cursor pointer, min-height 44px
- Disabled: quando `!selectedAccountId || !message.trim() || loading` — opacity 0.7, cursor not-allowed
- Loading: texto "Gerando resposta..." com spinner CSS (border-top animation, inline, 16px) antes do texto
- Hover (quando enabled): background `var(--color-primary-hover)`
- onClick: chamar `playgroundApi.test(token, { accountId: selectedAccountId, message: message.trim() })`
  - Sucesso: setResponse(data)
  - Erro: setError(mensagem)

Area de resposta (renderizar apenas apos submissao):
- Container: background `var(--color-bg)`, border `1px solid var(--color-border)`, border-radius `var(--radius)`, padding 24px, marginTop 24px
- `<label>` com texto "Resposta simulada da IA" (Label, 0.75rem, muted, marginBottom 8px)
- Se loading: div com texto "Gerando resposta..." (Body, 0.875rem, muted) com cursor pulsante (animacao blink, char "|" no final)
- Se response: texto da resposta (Body, 0.875rem, color `var(--color-text)`), white-space pre-wrap
- Se response: linha de meta-dados abaixo (Label, 0.75rem, muted, marginTop 16px):
  - "Tokens: {tokensUsed} | Latencia: {latencyMs}ms"
- Se error: texto vermelho com `var(--color-error)`

Cursor pulsante CSS (adicionar inline style ou no componente):
```css
@keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
```

**2. `frontend/app/(admin)/playground/page.tsx`** — Client Component ('use client')

Pagina wrapper:
```tsx
'use client';

import { PromptPlayground } from '@/components/PromptPlayground';

export default function PlaygroundPage(): JSX.Element {
  // TODO: obter token de autenticacao
  const token = '';

  return (
    <div>
      <header style={{ marginBottom: '32px' }}>
        <h1 style={{ fontSize: '1.75rem', fontWeight: 700, color: 'var(--color-text)' }}>
          Playground
        </h1>
        <p style={{ color: 'var(--color-text-muted)', marginTop: '4px', fontSize: '0.875rem' }}>
          Teste respostas da IA sem enviar mensagens reais
        </p>
      </header>
      <PromptPlayground token={token} />
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npm run lint 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `PromptPlayground.tsx` tem `'use client'`, importa `playgroundApi` e `accountsApi` de `@/lib/api`
    - `PromptPlayground.tsx` importa `AlertTriangle` de `lucide-react`
    - `PromptPlayground.tsx` contem texto "Esta e uma simulacao. Nenhum DM real sera enviado."
    - `PromptPlayground.tsx` contem texto "Gerando resposta..." e "Testar resposta"
    - `PromptPlayground.tsx` contem `<label htmlFor` para select, textarea
    - `playground/page.tsx` existe em `frontend/app/(admin)/playground/`
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>Playground funcional — gestor pode testar prompts sem enviar DMs reais</done>
</task>

<task type="auto">
  <name>Task 2: Criar LogTable e pagina de logs</name>
  <files>
    frontend/components/LogTable.tsx,
    frontend/app/(admin)/logs/page.tsx
  </files>
  <read_first>
    - frontend/lib/api.ts (logsApi, accountsApi, ProcessingLog, InstagramAccount)
    - frontend/components/EmptyState.tsx
    - frontend/components/Skeleton.tsx
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (Copywriting M6 — logs, filtros)
  </read_first>
  <action>
**1. `frontend/components/LogTable.tsx`** — Client Component ('use client')

```typescript
interface LogTableProps {
  token: string;
}
export function LogTable({ token }: LogTableProps): JSX.Element
```

**Estado:**
```typescript
const [accounts, setAccounts] = useState<InstagramAccount[]>([]);
const [logs, setLogs] = useState<ProcessingLog[]>([]);
const [total, setTotal] = useState(0);
const [page, setPage] = useState(1);
const [loading, setLoading] = useState(true);
const [error, setError] = useState<string | null>(null);

// Filtros
const [filterAccountId, setFilterAccountId] = useState<string>('');
const [filterStatus, setFilterStatus] = useState<string>('');
const [filterStartDate, setFilterStartDate] = useState<string>('');
const [filterEndDate, setFilterEndDate] = useState<string>('');
```

pageSize fixo = 20.

**Fetch contas:** useEffect chama `accountsApi.list(token)` no mount.

**Fetch logs:** useEffect chama `logsApi.list(token, { accountId: filterAccountId || undefined, status: filterStatus || undefined, startDate: filterStartDate || undefined, endDate: filterEndDate || undefined, page, pageSize: 20 })` quando qualquer filtro ou page muda. Resetar page para 1 quando filtro muda.

**Barra de filtros:**
div com display flex, gap 16px, flex-wrap wrap, marginBottom 24px, align-items flex-end

Filtro por conta:
- `<label htmlFor="log-filter-account">` texto "Filtrar por conta" (Label, 0.75rem, muted)
- `<select id="log-filter-account">` com option default "Todas as contas"
- Mesmos estilos de select do Playground
- min-width 180px

Filtro por status:
- `<label htmlFor="log-filter-status">` texto "Filtrar por status" (Label, 0.75rem, muted)
- `<select id="log-filter-status">` com options: "Todos", "Sucesso" (value="success"), "Erro" (value="error"), "Timeout" (value="timeout")
- min-width 140px

Filtro por periodo:
- `<label>` texto "Periodo" (Label, 0.75rem, muted)
- div flex, gap 8px
- `<input type="date" id="log-filter-start">` para data inicio
- `<input type="date" id="log-filter-end">` para data fim
- Inputs date: background `var(--color-bg)`, border `1px solid var(--color-border)`, border-radius `var(--radius)`, padding `8px 16px`, color `var(--color-text)`, fontSize 0.875rem, color-scheme dark

**Tabela:**
- `<table>` com width 100%, border-collapse collapse
- `<thead>` com `<th scope="col">`: "Conta", "Status", "Conversa", "Tempo", "Data"
- th: fontSize 0.75rem, fontWeight 700, color `var(--color-text-muted)`, padding `8px 16px`, text-align left, border-bottom `1px solid var(--color-border)`
- td: fontSize 0.875rem, padding `8px 16px`, border-bottom `1px solid var(--color-border)`

Colunas:
- Conta: `@{accountUsername}` (truncar se > 20 chars)
- Status badge (mesma logica do QueueMonitor):
  - success: background `var(--color-success)` opacity 0.15, color `var(--color-success)`, texto "Sucesso"
  - error: background `var(--color-error)` opacity 0.15, color `var(--color-error)`, texto "Erro"
  - timeout: background `var(--color-warning)` opacity 0.15, color `var(--color-warning)`, texto "Timeout"
  - Badge: padding `2px 8px`, border-radius 999px, fontSize 0.75rem
- Conversa: conversationId truncado (8 chars + ellipsis)
- Tempo: `{processingTimeMs}ms`
- Data: formatada com Intl.DateTimeFormat('pt-BR', { dateStyle: 'short', timeStyle: 'medium' })

Se log.status === 'error' e log.errorMessage: linha expandida abaixo com mensagem de erro (fontSize 0.75rem, color `var(--color-error)`, padding `4px 16px 8px 16px`, colspan 5)

**Paginacao:** Mesmo estilo da ConversationTable — "Anterior", "Pagina X de Y", "Proxima"

**Container:** background `var(--color-surface)`, border, border-radius, padding 24px. Overflow-x auto para mobile.

**Loading:** 5 Skeleton rows
**Empty:** EmptyState heading "Nenhum log encontrado para os filtros selecionados", body "Ajuste os filtros acima ou selecione um periodo maior."
**Error:** Banner vermelho com retry

**2. `frontend/app/(admin)/logs/page.tsx`** — Client Component ('use client')

```tsx
'use client';

import { LogTable } from '@/components/LogTable';

export default function LogsPage(): JSX.Element {
  // TODO: obter token de autenticacao
  const token = '';

  return (
    <div>
      <header style={{ marginBottom: '32px' }}>
        <h1 style={{ fontSize: '1.75rem', fontWeight: 700, color: 'var(--color-text)' }}>
          Logs de Processamento
        </h1>
        <p style={{ color: 'var(--color-text-muted)', marginTop: '4px', fontSize: '0.875rem' }}>
          Historico de processamento de mensagens do SAC
        </p>
      </header>
      <LogTable token={token} />
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npm run lint 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `LogTable.tsx` tem `'use client'`, importa `logsApi` e `accountsApi` de `@/lib/api`
    - `LogTable.tsx` contem 3 selects/inputs de filtro (account, status, date)
    - `LogTable.tsx` contem `<table>` com `<thead>` e 5 `<th scope="col">`
    - `LogTable.tsx` contem `<label htmlFor` para cada filtro
    - `LogTable.tsx` renderiza badges de status com 3 cores (success/error/timeout)
    - `logs/page.tsx` existe em `frontend/app/(admin)/logs/`
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>Logs de processamento com filtros e playground de prompts — M6 completo</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` retorna 0 erros
- `ls frontend/app/\(admin\)/playground/page.tsx` existe
- `ls frontend/app/\(admin\)/logs/page.tsx` existe
- `grep -r "playgroundApi" frontend/components/PromptPlayground.tsx` encontra match
- `grep -r "logsApi" frontend/components/LogTable.tsx` encontra match
- `grep "simulacao" frontend/components/PromptPlayground.tsx` encontra aviso
</verification>

<success_criteria>
- Playground em /admin/playground: selecionar conta, digitar mensagem, ver resposta da IA
- Aviso visivel de que e simulacao
- Logs em /admin/logs: tabela paginada com filtros por conta, status e periodo
- Status badges coloridos (success/error/timeout)
- Erros de processamento expandiveis na tabela
- Mobile responsive (tabela scrollavel, filtros empilhados)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-paineis/04-05-SUMMARY.md`
</output>
