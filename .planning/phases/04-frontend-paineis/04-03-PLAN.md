---
phase: 04-frontend-paineis
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/app/(admin)/layout.tsx
  - frontend/components/SidebarNav.tsx
  - frontend/app/(admin)/dashboard/page.tsx
  - frontend/components/QueueMonitor.tsx
autonomous: true
requirements: [FE-03, FE-05, FE-06]

must_haves:
  truths:
    - "Admin layout tem sidebar com 5 itens de navegacao"
    - "Sidebar colapsa em menu hamburguer em mobile (<768px)"
    - "Dashboard do admin mostra status do sistema e estatisticas da fila"
    - "QueueMonitor faz polling a cada 10s e mostra ultimo update"
  artifacts:
    - path: "frontend/components/SidebarNav.tsx"
      provides: "Navegacao lateral do admin"
      exports: ["SidebarNav"]
    - path: "frontend/components/QueueMonitor.tsx"
      provides: "Monitor de fila BullMQ com polling"
      exports: ["QueueMonitor"]
    - path: "frontend/app/(admin)/layout.tsx"
      provides: "Layout admin com sidebar"
      min_lines: 40
    - path: "frontend/app/(admin)/dashboard/page.tsx"
      provides: "Dashboard do admin com status e fila"
      min_lines: 60
  key_links:
    - from: "frontend/components/QueueMonitor.tsx"
      to: "frontend/lib/api.ts"
      via: "queueApi.getStats, queueApi.getJobs"
      pattern: "queueApi\\."
    - from: "frontend/components/SidebarNav.tsx"
      to: "next/navigation"
      via: "usePathname para item ativo"
      pattern: "usePathname"
---

<objective>
Refatorar layout admin com sidebar navegavel e implementar dashboard do gestor (M6) com status do sistema e monitor de fila BullMQ em tempo real.

Purpose: Base do M6 — sidebar e dashboard sao a porta de entrada do gestor.
Output: Layout admin funcional com sidebar + dashboard com QueueMonitor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-paineis/04-UI-SPEC.md
@frontend/app/(admin)/layout.tsx
@frontend/app/(admin)/dashboard/page.tsx
@frontend/lib/api.ts
@frontend/components/StatusCard.tsx
@frontend/components/Skeleton.tsx
@frontend/app/globals.css
</context>

<interfaces>
<!-- Tipos do api.ts que este plano consome (criados no plano 01) -->

```typescript
export interface QueueStats {
  waiting: number;
  active: number;
  failed: number;
  completed: number;
}

export interface QueueJob {
  id: string;
  name: string;
  status: 'waiting' | 'active' | 'completed' | 'failed';
  data: Record<string, unknown>;
  failedReason?: string;
  processedOn?: string;
  finishedOn?: string;
  timestamp: string;
}

export const queueApi = {
  getStats: (token: string) => request<{ data: QueueStats }>('/queue/stats', { token }),
  getJobs: (token: string, params?) => request<{ data: QueueJob[]; total: number }>('/queue/jobs', { token }),
};

export const healthApi = {
  check: () => request<{ status: string; timestamp: string }>('/health'),
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar SidebarNav e refatorar admin layout com sidebar</name>
  <files>frontend/components/SidebarNav.tsx, frontend/app/(admin)/layout.tsx</files>
  <read_first>
    - frontend/app/(admin)/layout.tsx (layout atual — top nav only, sem sidebar)
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (secoes Layout M6, Responsividade, Copywriting M6 nav items)
    - frontend/app/globals.css (CSS variables)
  </read_first>
  <action>
**1. `frontend/components/SidebarNav.tsx`** — Client Component ('use client')

Importar icones de lucide-react: `LayoutDashboard`, `Instagram`, `UserCircle`, `MessageSquare`, `FileText`, `Menu`, `X`

```typescript
export function SidebarNav(): JSX.Element
```

**Itens de navegacao (array constante no componente):**
```typescript
const navItems = [
  { href: '/admin/dashboard', label: 'Dashboard', icon: LayoutDashboard },
  { href: '/admin/accounts', label: 'Contas', icon: Instagram },
  { href: '/admin/personas', label: 'Personas', icon: UserCircle },
  { href: '/admin/playground', label: 'Playground', icon: MessageSquare },
  { href: '/admin/logs', label: 'Logs', icon: FileText },
];
```

**Estado:** `isOpen` (boolean, default false) — controla menu mobile

**Desktop (>= 768px):**
- Sidebar fixa a esquerda: width 240px, height `calc(100vh - 56px)`, position sticky, top 56px
- Background: `var(--color-surface)`
- Border-right: `1px solid var(--color-border)`
- Padding-top: 16px
- Cada item: display flex, align-items center, gap 8px, padding `8px 16px`, margin `0 8px`, border-radius `var(--radius)`, fontSize 0.875rem, color `var(--color-text-muted)`, transition background 0.15s
- Item ativo (pathname.startsWith(href)): background `var(--color-primary)` opacity 0.15, color `var(--color-primary)`, fontWeight 700 — NAO usar background solid primary, apenas tint
- Item hover (nao ativo): background `var(--color-border)` opacity 0.3
- Icone: size 20px
- Focus: outline `2px solid var(--color-primary)`, outline-offset 2px
- Usar `usePathname()` de `next/navigation` para determinar item ativo
- Usar `<Link>` de `next/link` para cada item

**Mobile (< 768px):**
- Sidebar oculta por padrao (display none)
- Botao hamburguer (Menu icon, 24px) visivel no topo da sidebar area — sera posicionado pelo layout
- Quando `isOpen = true`: sidebar aparece como overlay (position fixed, top 56px, left 0, width 240px, height calc(100vh - 56px), z-index 50, background var(--color-surface), box-shadow 4px 0 24px rgba(0,0,0,0.5))
- Botao X (X icon) no topo da sidebar aberta para fechar
- Clicar em um link fecha o menu (setIsOpen(false))
- Overlay backdrop (div com position fixed, inset 0, top 56px, background rgba(0,0,0,0.5), z-index 40) — clicar fecha

**Expor tambem:**
```typescript
export function SidebarToggle({ onClick }: { onClick: () => void }): JSX.Element
```
Botao com icone Menu (24px), visivel apenas em mobile (display none em >= 768px via media query inline ou style condicional). Usar `aria-label="Abrir menu"`.

NOTA: Para coordenar o toggle entre SidebarNav e o layout, o estado `isOpen` e o `setIsOpen` devem ser gerenciados pelo SidebarNav internamente. O SidebarToggle sera renderizado DENTRO do SidebarNav, posicionado absolutamente ou via portal. Alternativa mais simples: SidebarNav renderiza o botao hamburguer como parte de si mesmo, posicionado fora da sidebar via CSS.

Abordagem simplificada: SidebarNav renderiza TUDO — o botao hamburguer em mobile fica no canto superior esquerdo da area de conteudo (position fixed, top 64px, left 8px, z-index 30, visivel apenas em < 768px). A sidebar em si e o overlay.

**2. Refatorar `frontend/app/(admin)/layout.tsx`**

Converter para Client Component ('use client') pois usa SidebarNav que tem estado.

MANTER a top nav existente (brand + badge "Gestor") — apenas ajustar para funcionar com sidebar.

Nova estrutura:
```tsx
<div className="admin-layout">
  <nav className="admin-nav">
    {/* Brand + Badge — MANTER */}
  </nav>
  <div className="admin-body">
    <SidebarNav />
    <main className="admin-main">{children}</main>
  </div>
</div>
```

Estilos atualizados:
- `.admin-body`: display flex, flex 1, min-height `calc(100vh - 56px)`
- `.admin-main`: flex 1, padding `32px 24px`, max-width 1280px, width 100%
- Em mobile: `.admin-main` ocupa full width (sidebar e overlay)
- REMOVER metadata export (Client Component nao suporta). Mover metadata para um arquivo `frontend/app/(admin)/metadata.ts` se necessario, ou simplesmente remover (o title template pode ficar no root layout).

Badge "Gestor": manter cor `var(--color-primary)`, fontWeight 700 (nao 600 — conforme UI-SPEC typography, so 400 e 700).
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `SidebarNav.tsx` tem `'use client'`, importa de `lucide-react`, usa `usePathname` de `next/navigation`
    - `SidebarNav.tsx` contem os 5 navItems: Dashboard, Contas, Personas, Playground, Logs
    - `layout.tsx` tem `'use client'`, importa e renderiza `SidebarNav`
    - `layout.tsx` contem div com classe `admin-body` com display flex
    - Sidebar width e 240px em desktop
    - Badge usa fontWeight 700 (nao 600)
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>Layout admin refatorado com sidebar navegavel e responsiva</done>
</task>

<task type="auto">
  <name>Task 2: Implementar dashboard admin com StatusCards e QueueMonitor</name>
  <files>frontend/components/QueueMonitor.tsx, frontend/app/(admin)/dashboard/page.tsx</files>
  <read_first>
    - frontend/app/(admin)/dashboard/page.tsx (placeholder atual com StatusCard inline)
    - frontend/components/StatusCard.tsx (componente compartilhado do plano 01)
    - frontend/components/Skeleton.tsx
    - frontend/lib/api.ts (healthApi.check, queueApi.getStats, queueApi.getJobs)
    - .planning/phases/04-frontend-paineis/04-UI-SPEC.md (secoes M6 Layout, Copywriting M6, Interacoes — polling)
  </read_first>
  <action>
**1. `frontend/components/QueueMonitor.tsx`** — Client Component ('use client')

```typescript
export function QueueMonitor({ token }: { token: string }): JSX.Element
```

**Estado:**
```typescript
const [stats, setStats] = useState<QueueStats | null>(null);
const [jobs, setJobs] = useState<QueueJob[]>([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState<string | null>(null);
const [lastUpdated, setLastUpdated] = useState<Date | null>(null);
```

**Polling:** useEffect com setInterval a cada 10000ms. Chamar `queueApi.getStats(token)` e `queueApi.getJobs(token, { pageSize: 10 })`. Atualizar `lastUpdated` com `new Date()` a cada fetch com sucesso. Cleanup: clearInterval no return.

**Layout do QueueMonitor:**

Cabecalho: div flex, justify-content space-between, align-items center
- h2 "Monitor da fila" (Heading, 1.1rem, 700)
- Span "Atualizado ha Xs" (Label, 0.75rem, 400, muted) — calcular diferenca em segundos entre now e lastUpdated. Atualizar a cada segundo via outro setInterval de 1000ms.

Linha de stats: grid com 4 colunas (repeat(4, 1fr)), gap 16px, marginTop 16px, marginBottom 24px
Cada stat e um mini card:
- Background: `var(--color-bg)` (diferente do container que e surface)
- Padding: 16px, border-radius `var(--radius)`, text-align center
- Label (0.75rem, 400, muted): "Aguardando", "Processando", "Com falha", "Concluidos"
- Valor (1.75rem, 700): numero formatado com `toLocaleString('pt-BR')`
- Cor do valor: waiting = `var(--color-warning)`, active = `var(--color-primary)`, failed = `var(--color-error)`, completed = `var(--color-success)`

Tabela de jobs: mesma estrutura da ConversationTable
- Colunas: "ID", "Nome", "Status", "Criado em"
- ID: truncar em 8 chars com ellipsis
- Status badge: span com padding `2px 8px`, border-radius 999px, fontSize 0.75rem
  - waiting: background `var(--color-warning)` opacity 0.15, color `var(--color-warning)`, texto "Aguardando"
  - active: background `var(--color-primary)` opacity 0.15, color `var(--color-primary)`, texto "Processando"
  - completed: background `var(--color-success)` opacity 0.15, color `var(--color-success)`, texto "Concluido"
  - failed: background `var(--color-error)` opacity 0.15, color `var(--color-error)`, texto "Falhou"
- Se job.failedReason: mostrar em linha expandida abaixo, fontSize 0.75rem, color `var(--color-error)`
- Timestamp: formatar com Intl.DateTimeFormat('pt-BR', { dateStyle: 'short', timeStyle: 'medium' })

Container geral: background `var(--color-surface)`, border, border-radius, padding 24px

Loading: Skeleton grid (4 stats) + 5 Skeleton rows
Error: "Nao foi possivel ler o estado da fila. Verifique se o Redis esta em execucao." com botao retry
Em mobile: stats grid vira 2 colunas, tabela scrollavel horizontal

**2. Reescrever `frontend/app/(admin)/dashboard/page.tsx`**

Client Component ('use client') — gerencia fetch de dados.

**Estrutura:**
```
header (marginBottom 32px)
  h1 "Painel do Gestor" (Display: 1.75rem, 700)
  p "Visao geral do sistema SAC Automatico" (Body: 0.875rem, 400, muted, marginTop 4px)

StatusCards grid (marginBottom 32px)
  grid: repeat(auto-fill, minmax(240px, 1fr)), gap 24px
  4 StatusCards:
    1. title="Status do Sistema" value={healthStatus} description="Backend API" borderColor="var(--color-success)" ou "var(--color-error)"
    2. title="Jobs Aguardando" value={stats.waiting} description="Na fila de processamento" borderColor="var(--color-warning)"
    3. title="Jobs com Falha" value={stats.failed} description="Requerem atencao" borderColor="var(--color-error)"
    4. title="Taxa de Sucesso" value={successRate}% description="Jobs concluidos sem falha" borderColor="var(--color-success)"
  Loading: 4 Skeleton cards

QueueMonitor (full width)
```

**Estado e fetch:**
- Fetch `healthApi.check()` para status do sistema. Se sucesso: "Sistema operacional". Se erro: "Offline".
- Fetch `queueApi.getStats(token)` para stats iniciais dos StatusCards.
- Token: string vazia '' por enquanto. Comentario `// TODO: obter token de autenticacao`.
- Calcular successRate: `stats.completed > 0 ? Math.round((stats.completed / (stats.completed + stats.failed)) * 100) : 0`

REMOVER StatusCard inline que existia no placeholder — agora importar de `@/components/StatusCard`.
REMOVER metadata export (Client Component).
  </action>
  <verify>
    <automated>cd /Users/danilomartins/Documents/sac-automatico/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `QueueMonitor.tsx` tem `'use client'`, importa `queueApi` e `QueueStats` de `@/lib/api`
    - `QueueMonitor.tsx` contem `setInterval` com 10000 (polling 10s)
    - `QueueMonitor.tsx` contem texto "Atualizado ha"
    - `dashboard/page.tsx` tem `'use client'`, importa StatusCard de `@/components/StatusCard`
    - `dashboard/page.tsx` importa QueueMonitor de `@/components/QueueMonitor`
    - `dashboard/page.tsx` contem texto "Painel do Gestor" e "Visao geral do sistema SAC Automatico"
    - `dashboard/page.tsx` importa `healthApi` de `@/lib/api`
    - Nenhum StatusCard inline (funcao local) no arquivo page.tsx
    - Zero erros TypeScript
  </acceptance_criteria>
  <done>Dashboard admin com 4 StatusCards + QueueMonitor com polling de 10s funcional</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` retorna 0 erros
- `grep -r "usePathname" frontend/components/SidebarNav.tsx` encontra match
- `grep -r "setInterval" frontend/components/QueueMonitor.tsx` encontra match
- Layout admin renderiza SidebarNav com 5 itens
- Dashboard admin renderiza 4 StatusCards + QueueMonitor
</verification>

<success_criteria>
- Admin layout tem sidebar com 5 itens navegaveis
- Sidebar colapsa em mobile (<768px) com menu hamburguer
- Dashboard admin mostra status do sistema via healthApi
- QueueMonitor faz polling a cada 10s e exibe stats + jobs
- Todos os textos conforme copywriting contract M6
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-paineis/04-03-SUMMARY.md`
</output>
